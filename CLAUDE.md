# Before working on this mod

Read `C:/ai-workspace/HumanHost Audit/MOD_CONVENTIONS.md` first - the authoritative, cross-mod
list of lessons learned across every Human Host BepInEx mod (loot tag localization, HDRP material
gotchas, debug key handling, template-cloning pattern, mesh pipeline pitfalls). Check this mod's
own code against that list, don't just skim it once.

When you learn something new here that would apply to other Human Host mods too, add it to that
same file immediately - it's the shared reference, not something to duplicate per-mod.
